import { ServicePage } from "@/components/ServicePage";

export const metadata = { title: "Private Relocation" };

export default function PrivatRelocationPage() {
  return (
    <ServicePage
      locale="en"
      pageKey="umzug"
      currentPath="/en/privat-relocation"
    />
  );
}
