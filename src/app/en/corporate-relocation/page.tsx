import { ServicePage } from "@/components/ServicePage";

export const metadata = { title: "Corporate Relocation" };

export default function CorporateRelocationPage() {
  return (
    <ServicePage
      locale="en"
      pageKey="firmenumzug"
      currentPath="/en/corporate-relocation"
    />
  );
}
